<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vehicle Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-center mb-8">Vehicle Management System</h1>
        
        <div class="mb-6">
            <a href="/vehicles/new" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">Add New Vehicle</a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% vehicles.forEach(vehicle => { %>
                <div class="bg-white rounded-lg shadow-md overflow-hidden">
                    <img src="<%= vehicle.image %>" alt="<%= vehicle.name %>" class="w-full h-48 object-cover">
                    <div class="p-4">
                        <h2 class="text-xl font-semibold"><%= vehicle.name %></h2>
                        <p class="text-gray-600"><%= vehicle.brand %></p>
                        <p class="text-lg font-bold text-blue-600">$<%= vehicle.price.toLocaleString() %></p>
                        <p class="text-gray-700 mt-2"><%= vehicle.description %></p>
                        
                        <div class="mt-4 flex space-x-2">
                            <a href="/vehicles/<%= vehicle._id %>" class="text-sm bg-green-500 text-white px-3 py-1 rounded hover:bg-green-600 transition">View</a>
                            <a href="/vehicles/<%= vehicle._id %>/edit" class="text-sm bg-yellow-500 text-white px-3 py-1 rounded hover:bg-yellow-600 transition">Edit</a>
                            <form action="/vehicles/<%= vehicle._id %>?_method=DELETE" method="POST" class="inline">
                                <button type="submit" class="text-sm bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600 transition">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
